{
  "note": "Generated by core/tools/gen_vectors.py. The chain must verify in both implementations; every directory under tampered/ must fail in both, for the reason named.",
  "chain": {
    "records": 3,
    "anchored_to": 0,
    "must_verify": true
  },
  "tampered": {
    "rewritten_result": "signature no longer covers the body",
    "deleted_middle": "chain replay finds a gap",
    "reordered": "sequence and prev_record_hash disagree"
  }
}
